# Example Tests

This directory is part of the repository example area.

For the current release, the validated test entrypoint is the model-generation path:

```bash
cd /app
pytest example/tests/test_models.py -q
```

Legacy gateway and generated-client workflows are no longer part of the supported release validation path.
